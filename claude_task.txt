請讀取並執行以下開發指令：

請針對以下三個效能問題進行優化，請先閱讀相關檔案再修改：

1. **Nested Loop in Inventory Rendering**
找出遺物清單（inventory）渲染函式中的巢狀迴圈，改用 Map 或預先建立查找表（lookup table）來取代內層迴圈，降低時間複雜度。

2. **Nested Loop in Shop Rendering**
找出商店（shop）渲染函式中的巢狀迴圈，同樣改用 Map 或查找表取代，避免每次渲染都重複遍歷。

3. **Unnecessary DOM Query in Loop**
找出所有在迴圈內重複呼叫 document.getElementById、document.querySelector 或 document.querySelectorAll 的地方，將 DOM 查詢移到迴圈外部，用變數快取結果。

請逐一說明每個問題在哪個檔案哪個函式、原本的寫法是什麼、改成什麼，修改完後做一次摘要報告。