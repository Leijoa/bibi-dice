請讀取並執行以下開發指令：

請修改 css/style.css，將重骰按鈕的小骰子 icon 動畫從 hover 觸發改為「待機呼吸搖晃」效果：

1. 刪除舊的 hover 動畫規則，找到並刪除這段：
.btn-roll:not(:disabled):hover .btn-roll-icon {
  animation: rollJiggle 0.08s linear infinite alternate;
}

2. 在 .btn-roll .btn-roll-icon 的規則下方新增以下內容：

@keyframes dice-idle-sway {
  0%   { transform: rotate(-8deg) scale(0.95); }
  50%  { transform: rotate(8deg)  scale(1.05); }
  100% { transform: rotate(-8deg) scale(0.95); }
}

.btn-roll:not(:disabled) .btn-roll-icon {
  animation: dice-idle-sway 1.8s ease-in-out infinite;
}

.btn-roll:disabled .btn-roll-icon {
  animation: none;
  transform: none;
}

修改完成後告訴我。